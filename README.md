# sentient-qa
Core service for AI-driven, no-code test automation. Resolves UI elements from natural-language instructions using a tiered pipeline - cache, heuristic matching, text LLM, and vision LLM, so engineers never write or maintain locators. Platform-agnostic, extensible, built for scale.
